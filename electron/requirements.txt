# NXS+ Sphere Network — Python server requirements
# Install with: pip install -r requirements.txt

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Optional: local Whisper transcription (recommended)
# Comment out if you want to use OpenAI API instead
faster-whisper>=0.10.0

# Optional: GPU acceleration for Whisper
# Uncomment if you have an NVIDIA GPU with CUDA
# torch>=2.0.0
# torchaudio>=2.0.0
